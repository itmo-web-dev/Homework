'use strict';

var a = parseInt(prompt('Выберите наименьшее число 2, 6, 8'));
    
    if (a == 2) {
        console.log ('Верно');    
    }
    else if (a != 2) {
        console.log ('Не верно, введите еще раз');
    }
